# {算子名称} — 算法说明模板

## 概述

简要描述算子的功能、适用场景和在化工模拟/预测中的定位（~100字）。

## 算法原理

### 问题定义

给出算子求解的数学问题定义，包括输入、输出和约束条件。

### 核心公式

$$
\text{核心公式，如总势能函数：} E_{\text{total}} = \sum E_{\text{term}}
$$

逐项展开说明每个子项的计算公式：

1. **{第1项名称}**
   $$
   E_1 = \sum f(x_i)
   $$
   参数含义说明，力/梯度推导。

2. **{第2项名称}**
   $$
   E_2 = \sum g(y_j)
   $$

### 算法流程

```
Input:  {输入数据}
Output: {输出数据}

1. 步骤一描述
2. 步骤二描述
3. 步骤三描述
```

### 边界条件

说明周期性边界条件（PBC）、截断、排除对等特殊处理。

## NPU 实现

### 算子拆分策略

描述如何将计算任务拆分到各 AI Core，包括 tile 策略、数据分块方式。

### Kernel 实现要点

- 关键数据结构
- 数学函数替代方案（如 Newton-Raphson 迭代替代 `sin/cos/sqrt`）
- 同步/通信机制
- 中间变量的精度和存储

### Host 侧流程

描述 Host 侧调度逻辑，包括循环控制、收敛判断、内存管理等。

## 精度分析

| 量 | NPU float32 | CPU double (参考) | 误差 |
|---|---|---|---|
| {示例量1} | {值} | {值} | {误差} |
| {示例量2} | {值} | {值} | {误差} |

说明误差来源和控制措施。

## 性能数据

| 输入规模 | CPU 耗时 | NPU 耗时 | 加速比 |
|----------|----------|----------|--------|
| 规模1 | {值} ms | {值} ms | {值}x |
| 规模2 | {值} ms | {值} ms | {值}x |

## 参考

1. Author, A. B.; Author, C. D. *Journal Name* **Year**, *Volume*, Pages.
2. Author, E. F. *Book Title*; Publisher, Year.