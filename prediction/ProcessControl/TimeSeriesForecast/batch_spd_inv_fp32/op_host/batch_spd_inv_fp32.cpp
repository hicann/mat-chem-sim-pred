/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

// Host scaffold generated by CANN msopgen 8.1.RC1; tiling and inference are operator-specific.
#include "batch_spd_inv_fp32_tiling.h"
#include "../../common/op_host_utils.h"

namespace optiling {
ge::graphStatus BatchSpdInvFp32Tiling(gert::TilingContext* context)
{
    BatchSpdInvFp32TilingData tiling;
    const auto& inputShape = context->GetInputShape(0)->GetStorageShape();
    const size_t rank = inputShape.GetDimNum();
    const uint32_t matrixSize = static_cast<uint32_t>(inputShape.GetDim(rank - 1));

    uint32_t batch = 1U;
    for (size_t dimension = 0; dimension + 2 < rank; ++dimension) {
        batch *= static_cast<uint32_t>(inputShape.GetDim(dimension));
    }
    tiling.set_batch(batch);
    tiling.set_m(matrixSize);

    const uint32_t coreCount = timeseries_host::GetVectorCoreCount(context);
    context->SetBlockDim(timeseries_host::SelectBlockDim(batch, coreCount));

    constexpr size_t systemWorkspaceBytes = static_cast<size_t>(16) * 1024 * 1024;
    context->GetWorkspaceSizes(1)[0] = systemWorkspaceBytes;

    timeseries_host::StoreTilingData(context, tiling);
    return ge::GRAPH_SUCCESS;
}
}  // namespace optiling

namespace ge {
graphStatus InferBatchSpdInvFp32Shape(gert::InferShapeContext* context)
{
    return timeseries_host::CopyFirstInputShape(context);
}

graphStatus InferBatchSpdInvFp32DataType(gert::InferDataTypeContext* context)
{
    return timeseries_host::CopyFirstInputDataType(context);
}
}  // namespace ge
